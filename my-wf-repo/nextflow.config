profiles {

    conda {
        process {
            conda {
                enabled = true
            }
        }
    }

    test {
        params.reads = "data/test/*_{R1,R2}.fastq.gz"
    }
}

process.executor = 'local'
